import { AbbrNumberPipe } from './abbr-number.pipe';

describe('AbbrNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new AbbrNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
